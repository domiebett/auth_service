version: "2"
services:
  user_service:
    container_name: user_service
    build: ./
    restart: always
    ports:
      - "3002:3000"
    depends_on:
      - user_service_db
    command: npm run dev
    volumes:
      - ./:/usr/src/app
      - /usr/src/app/node_modules
    networks:
      - budget_app_network
  user_service_db:
    container_name: user_service_db
    image: mysql:8.0
    command: --default-authentication-plugin=mysql_native_password
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: user_service_password
      MYSQL_DATABASE: user_service
      MYSQL_USER: user_service
      MYSQL_PASSWORD: user_service_password
    ports:
      - "3308:3306"
    networks:
      - budget_app_network
networks:
  budget_app_network:
    external: true
